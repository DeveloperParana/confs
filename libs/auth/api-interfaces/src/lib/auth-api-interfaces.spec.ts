import { authApiInterfaces } from './auth-api-interfaces';

describe('authApiInterfaces', () => {
  it('should work', () => {
    expect(authApiInterfaces()).toEqual('auth-api-interfaces');
  });
});
